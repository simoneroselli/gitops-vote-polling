---
apiVersion: v1
kind: Service
metadata:
  name: db     # CoreDNS will allow other apps to find this at "http://db:5432"
  namespace: default
spec:
  ports:
  - port: 5432
    targetPort: 5432
  selector:
    app: db